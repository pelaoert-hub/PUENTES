{
  "name": "puentes-app",
  "version": "1.0.0",
  "private": true,
  "description": "App de Puentes: guia y comunidad para cubanos dentro y fuera de la isla",
  "scripts": {
    "serve": "powershell -ExecutionPolicy Bypass -File tools/serve.ps1",
    "sync": "cap sync",
    "android": "cap open android",
    "ios": "cap open ios",
    "deploy:preview": "vercel deploy web",
    "deploy:prod": "vercel deploy web --prod"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "vercel": "^59.5.0"
  }
}
